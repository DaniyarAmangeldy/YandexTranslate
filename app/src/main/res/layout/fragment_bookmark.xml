<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:context="daniyaramangeldy.yandextranslate.ui.fragment.FragmentBookmark">

            <android.support.design.widget.TabLayout
                android:id="@+id/fragment_bookmark_tablayout"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                style="@style/tabLayoutStyle"
                app:tabSelectedTextColor="@color/white"
                app:tabIndicatorColor="@color/white"
                android:elevation="2dp"
                />


        <android.support.v4.view.ViewPager
            android:id="@+id/fragment_bookmark_pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/fragment_bookmark_tablayout" />

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/bookmarks_btn_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            app:fabSize="normal"
            android:layout_margin="16dp"
            android:layout_alignParentBottom="true"
            android:src="@android:drawable/ic_menu_delete"
            />

    </RelativeLayout>
</layout>